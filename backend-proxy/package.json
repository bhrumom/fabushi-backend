{
  "name": "global-broadcast-proxy",
  "version": "1.0.0",
  "description": "一个本地代理服务器，用于将来自Flutter Web的HTTP请求转换为UDP数据包进行全球发送。",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "CodeBuddy",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1",
    "dgram": "^1.0.1"
  }
}